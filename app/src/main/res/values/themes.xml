<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FinalProjectApartTalk" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- 상태바의 색상을 바꿔보자 -->
        <item name="colorPrimaryDark">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.FinalProjectApartTalk" parent="Base.Theme.FinalProjectApartTalk" />

    <style name="Theme.MyApplicationCheck" parent="Base.Theme.FinalProjectApartTalk" />

    <style name="Base.Theme.ApartTalk" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>